---
import ImageWrapper from './ImageWrapper.astro';

interface Props {
  src: string;
  alt?: string;
  title?: string;
  class?: string;
  /* 캡션은 remark 플러그인이 data-caption 속성으로 넘겨준다. */
  'data-caption'?: string;
}

const { src, alt = '', title, class: className } = Astro.props;

// Extract caption from title attribute or data-caption
const caption = title || Astro.props['data-caption'] || '';
---

<ImageWrapper
  src={src}
  alt={alt}
  caption={caption}
  class={className}
  width={800}
  height={450}
  format="webp"
  quality={85}
  loading="lazy"
/>
